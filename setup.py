from setuptools import setup, find_packages

setup(
    name="vips_data_analysis",  # Name of your package
    version='0.0.0',
    packages=find_packages(),  # This will automatically find the 'classes' package
)
